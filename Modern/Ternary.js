// Ternary operator:
// condition ? expr1 : expr2

var age = 20;
var canVote = age >= 18 ? 'Yes' : 'no';

console.log(canVote); // yes

document.querySelector('.result').innerHTML = canVote;
